<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link width="" rel="shortcut icon" href="./images/logo.jpeg" />

	<title>Mi Tiendita</title>
	<!-- Font Awesome Stylesheet -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="./css/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/app.css">
</head>

<body>
	<!-- Header -->
	<header class="main-header">
		<% if (locals.user != null) {%>
			<%- include('./partials/headerLogeado')%>
		<% }%>
		<% if (locals.user == null) {%>
			<%- include('./partials/header')%>
		<% }%>
	</header>
	<!-- /Header -->

	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h1 class="products-title">Resultados para tu busqueda: </h1>
			</div>
			<!-- Show the "ultimos" products -->
			<% if (data.length == 0) { %>
        <h2 style="color:black"> No exite ningún usuario para tu búsqueda</h2>
		<% } %>
		<% for( let i = 0; i < data.length; i++ ) { %>
            <section>
			<a href="/users/profile/<%=data[i].id%>"><div class="img-container offset-3 col-2">
                <img src="<%= data[i].foto_perfil %>" alt="foto de perfil" class="foto-perfil">
            </div>
            <h1 class="main-title"> <%= data[i].username %>
            </h1></a></section>
		<% } %>
	<!-- footer -->
	<footer class="main-footer">
		<%- include('./partials/footer')  %>
	</footer>
	<!-- /footer -->
</body>

</html>